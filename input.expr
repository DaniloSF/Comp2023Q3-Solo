programa
	REAL a,b,d.
	INTEGER e,c.
	escreva("Hello World!").
	escreva("Digite o valor C").
	leia(c).
	
	a := 2.0.
	b := 3.2 + 2.0 - 1.0 + 5.0 - 3.0 + a.
	c := e - 1.
	escreva(a).
	escreva(b).
	se (a >= b) entao {
		escreva(a).
	} senao {
		se (a < b) entao {
			escreva(a).
			escreva("goodbye world").
		} senao {
			escreva("nao sei").
		}.
	}.
	escreva("Hmm").
	se (a < b) entao {
	    escreva("WOW").
	}.
	escreva("Oi").
fimprog.